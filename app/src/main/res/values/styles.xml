<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="popupMenuBackground">@android:color/white</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="myanimationWindow">
        <item name="android:windowEnterAnimation">@anim/phase_in</item>
        <item name="android:windowExitAnimation">@anim/phase_out</item>
    </style>
    <style name="MyCustomTheme">
        <item name="android:windowAnimationStyle">@style/myanimationWindow</item>
    </style>

    <style name="cart_dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/myanimationWindow</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
